<form method="post" action="{{ path('lot_habit_delete', {'id': lot_habit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lot_habit.id) }}">
    <button class="btn">Delete</button>
</form>
